using UnityEngine;

public class e3 : MonoBehaviour
{
    public Vector3 modificable;

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log(modificable);
    }
}
